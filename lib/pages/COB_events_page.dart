import 'package:flutter/material.dart';

class COBEventsPage extends StatelessWidget {
  COBEventsPage({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: ListView(
        children: [
          ListTile(title: 
          Text("COB EVENTS"),)
        ],
      ),
    );
  }
}