import 'package:flutter/material.dart';

class CCSEventsPage extends StatelessWidget {
  CCSEventsPage({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: ListView(
        children: [
          ListTile(title: 
          Text("CCS EVENTS"),)
        ],
      ),
    );
  }
}